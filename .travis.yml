dist: Trusty
language: python
python:
- '3.6'
before_install:
- openssl aes-256-cbc -K $encrypted_70ca040df3bb_key -iv $encrypted_70ca040df3bb_iv
  -in sql_config.test.json.enc -out src/db/sql_config.json -d
install:
- pip install -r requirements.txt
- pip install -U pytest
script:
- py.test
addons:
  ssh_known_hosts:
  - waterfall.wwyf.top
after_success:
- chmod +x ./deploy.sh && ./deploy.sh
